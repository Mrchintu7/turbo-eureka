class ChessPiece:
    def __init__(self, color):
        self.color = color

class Pawn(ChessPiece):
    def __init__(self, color):
        super().__init__(color)

    def move(self, start, end):
        # Implement pawn movement logic

# Repeat similar structures for other chess pieces

class ChessBoard:
    def __init__(self):
        # Initialize 2D array to represent the chessboard and place pieces

    def display(self):
        # Display the current state of the chessboard

    def is_valid_move(self, start, end):
        # Check if the move is valid based on the rules

# Example usage
board = ChessBoard()
board.display()

# Implement game loop with turn management and move validation
